{% extends 'base.html.twig' %}

{% block title %}FreshBurst - {{ title }}{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-4">{{ title }}</h1>
        {{ form_start(form) }}
            <div class="mb-3">
                {{ form_row(form.name, {'attr': {'class': 'form-control'}}) }}
            </div>
            <div class="mb-3">
                {{ form_row(form.image, {'attr': {'class': 'form-control'}}) }}
            </div>
            <button type="submit" class="btn btn-primary">Enregistrer</button>
        {{ form_end(form) }}
        <a href="{{ path('admin_categories') }}" class="btn btn-secondary mt-2">Retour à la liste</a>
    </div>
{% endblock %}